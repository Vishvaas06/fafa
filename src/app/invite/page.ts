import { redirect } from "next/navigation";

const InvitePage = () => {
  // needs to be replaced with auth link of client
  redirect("https://discord.com");
  return null;
};

export default InvitePage;
